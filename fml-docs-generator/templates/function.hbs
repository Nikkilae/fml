
`{{{return_type}}} {{name}}({{#each parameters as |p|}}{{{p.type}}} {{p.name}}{{#unless @last}}, {{/unless}}{{/each}})`

**Parameters:**

{{#if parameters}}
> | Name | Type | Description |
  |:-|:-|:-|
{{#each parameters as |p|}}
  | `{{p.name}}{{#if p.default_value}} = {{p.default_value}}{{/if}}` | `{{{p.type}}}` | {{p.description}} |
{{/each}}
{{/if}}

**Returns:** `{{{return_type}}}`

{{#if fmod_docs_link}}FMOD API documentation:<br/>[{{fmod_docs_link}}]({{fmod_docs_link}}){{/if}}

<br/><br/>

---
